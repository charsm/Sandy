---
import BlogLayout from "~/layouts/BlogLayout.astro";
import { Config } from "~/config";

export const getStaticPaths = async () => {
  return Config.categories.map((item) => {
    return {
      params: {
        cateId: item.key,
      },
    };
  });
};
---

<BlogLayout>ds</BlogLayout>
